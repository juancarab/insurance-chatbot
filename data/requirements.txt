# --- Config & Utilities ---
python-dotenv==1.2.1
boto3
requests==2.32.5

# --- Pipeline (EDA/Ingest) ---
pandas==2.3.3
pypdf==6.1.3
opensearch-py==2.8.0

# --- RAGAS & Tests ---
# Ragas is the main dependency that will set the langchain versions.
ragas==0.1.21
datasets==2.18.0
pytest==8.4.2

# --- Embeddings (HF, Transformers) ---
# We let pip find a compatible version
langchain-huggingface 
sentence-transformers==5.1.2